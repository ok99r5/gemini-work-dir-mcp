This adds and manages project memories in `.gemini/GEMINI.md` within the work directory.

## Usage with MCP

"mcpServers": {
  "Gemini Work Dir MD": {
    "command": "npx",
    "args": [
      "-y",
      "gemini-work-dir-mcp"
    ]
  }
}
